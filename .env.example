# CodeAI CLI Environment Configuration
# Copy this file to .env if you need to customize settings

# Environment Mode
# Set to 'development' for local testing, 'production' for end users
NODE_ENV=production

# Development URLs (only used when NODE_ENV=development)
# Uncomment and set these when developing locally
# CODEAI_WEB_URL=http://localhost:3000
# CODEAI_API_URL=http://localhost:5001/{PROJECT_ID}/{REGION}

# Logging Level (error, warn, info, debug)
LOG_LEVEL=info

# CLI Configuration
CLI_CONFIG_DIR=~/.codeai
CLI_TIMEOUT=120000

# Network Configuration
HTTP_TIMEOUT=30000
MAX_RETRIES=3
